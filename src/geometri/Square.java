package geometri;

import java.awt.Color;
import java.awt.Graphics;

public class Square extends Rectangle{
	
	/**
	 * Constructs a square with position (x,y) and with  specified side length and color
	 * @param x horizontal position
	 * @param y vertical position
	 * @param side side length of the rectangle
	 * @param c color of the square
	 * @throws IllegalPositionException
	 */
	public Square(int x, int y, int side, Color c) throws IllegalPositionException{
		
	}
	/**
	 * Constructs a square from a GeometricalForm and specified side length and color
	 * @param f GeometeicalForm to construct the oval from
	 * @param side side length of the rectangle
	 * @param c color of the square
	 */
	public Square(GeometricalForm f, int side, Color c){
		
	}
	/**
     * {@inheritDoc}
     */
	 public void fill(Graphics g){
	    	
	    }
	    
	   
	    
	    
	  
}
